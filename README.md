# upc-metadata-templates


This repository contains **standardized metadata templates** used across all UPC research outputs.  
It ensures that every paper, dataset, and OSF upload includes consistent and complete metadata.

---

## 📌 Purpose
- Provide ready-to-use JSON/YAML templates for metadata.
- Ensure consistency across OSF uploads and GitHub repos.
- Make UPC materials machine-readable and searchable.
- Support automation scripts that validate metadata.

---

## 🗂 Folder Structure
